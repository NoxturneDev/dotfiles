# Global Agent Rules

These are the absolute global runtime constraints for this machine's agentic
workspace. They apply to every Claude session regardless of working directory.

@/home/noxturne/agents/AGENTS.md
